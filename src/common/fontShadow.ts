import { customColor } from 'src/constants';

const basic = `1px 1px 1px`;

const bold = `1px 1px 5px`;


export const whiteShadow = `${customColor.white} ${basic}`;

export const whiteBoldShadow = `${customColor.white} ${bold}`;

export const purpleBoldShadow = `${customColor.purple} ${bold}`;

export const magentaBoldShadow = `${customColor.magenta} ${bold}`;

export const grayBoldShadow = `${customColor.gray} ${bold}`;